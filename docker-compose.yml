services:
  csmar-mcp:
    build: .
    image: csmar-mcp:local
    container_name: csmar-mcp
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - csmar_state:/var/lib/csmar-mcp

volumes:
  csmar_state:
