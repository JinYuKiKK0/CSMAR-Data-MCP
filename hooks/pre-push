#!/usr/bin/env bash
# Check-only lint before push. No write-back: push phase has no commit to
# attach fixes to, so leaving a dirty workspace would be confusing.
#
# Activate with: git config core.hooksPath hooks
set -e

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

uv run python scripts/check.py
